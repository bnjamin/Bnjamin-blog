-@articles.each do |article|
  .article
    %h1
      %a{:href => article.path}= article.title
    %span.date= article.date
    .content= article.body
    .more
      %a{:href => "article/#{article.path}#disqus_thread"} Comments
  .hr
